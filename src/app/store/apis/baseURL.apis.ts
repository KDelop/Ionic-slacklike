import { environment } from '@app/src/environments/environment'

export const authBaseUrl: string = environment.authServerURL

export const chatBaseUrl: string = environment.chatServerURL
// export const baseUrl = 'http://localhost:3030';
